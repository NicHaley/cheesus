<h2>Restaurant List</h2>
  
  <div class="restaurant-form">
      <div class="search">
      <%= search_form_for @search do |f| %>
      <div class="field">
        <%= f.text_field :name_cont %>
      </div><br/>
      <div class="actions"><%= button_tag "Search", class: "btn btn-warning" %></div>
      <% end %>
    </div>
  </div>

<table class="table table-hover">
  <thead>
    <tr>
      <th></th>
      <th><%= sort_link @search, :name, "Name" %></th>
      <th><%= sort_link @search, :cuisine_type, "Cuisine" %></th>
      <th><%= sort_link @search, :location, "Location" %></th>
      <th><%= sort_link @search, :price, "Price" %></th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>

    <tr>
      <% @restaurant.each do |restaurant| %>
      <td><%= link_to image_tag(restaurant.picture, :height=> "80"), "/restaurants/#{restaurant.id}" %></td>
      <td class="tr-restaurant"><%= restaurant.name %></td>
      <td class="tr-restaurant"><%= restaurant.cuisine_type %></td>
      <td class="tr-restaurant"><%= restaurant.location %></td>
      <td class="tr-restaurant">
        <% restaurant.price %>
        <% case restaurant.price %>
        <% when 1 %>
        <%= link_to image_tag("dollar1.png", :height=> "30"), "/" %>
        <% when 2 %>
        <%= link_to image_tag("dollar2.png", :height=> "30"), "/" %>
        <% when 3 %>
        <%= link_to image_tag("dollar3.png", :height=> "30"), "/" %>
        <% when 4 %>
        <%= link_to image_tag("dollar4.png", :height=> "30"), "/" %>
        <% when 5 %>
        <%= link_to image_tag("dollar5.png", :height=> "30"), "/" %>
        <% end %>
      </td>
      <td class="tr-restaurant"><%= link_to 'Show', restaurant %></td>

    </tr>
    <% end %>
  </tbody>
</table>

<% if r_current_user %>
<button type="button" class="btn btn-warning">
<%= link_to 'Add New Restaurant', new_restaurant_path, class: "btn-restaurant" %>
</button>
<% end %>